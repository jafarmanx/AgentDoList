<%
  config_path = if CurioArch.saas?
    gem_path = Gem::Specification.find_by_name("curio-arch-saas").gem_dir
    File.join(gem_path, "config", "database.yml")
  else
    File.join("config", "database.#{CurioArch.db_adapter}.yml")
  end
%>
<%= ERB.new(File.read(config_path)).result %>
